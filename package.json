{
  "name": "git-branch-helper",
  "version": "0.0.20",
  "description": "gbh stands for git branch help. utilities to copy changes from branch into master without merging",
  "scripts": {
    "start": "node src/index.js",
    "publish":"git push && npm publish && npm upgrade git-branch-helper -g"
  },
  "dependencies": {
    "chalk": "^1.1.1",
    "lodash": "^3.10.1"
  },
  "preferGlobal": true,
  "bin": {
    "gbh": "src/index.js"
  }
}
